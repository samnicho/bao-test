const Feature = {
  render: (title, desc) => `
    <div class="features_container__feature">
      <div>
        <h4>${title}</h4>
        <h6>${desc}</h6>
      </div>
    </div>
`};

export default Feature;